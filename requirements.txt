# requirements.txt for BitaxePID Auto-Tuner
#
# Requiere Python 3.9 o superior: el codigo usa genericos nativos
# (tuple[str, int], list[str]) sin from __future__ import annotations.

pyyaml>=6.0             # carga de la configuracion y de pools.yaml
urllib3>=1.26.0         # cliente HTTP contra la API del miner (BitaxeAPIClient)
simple-pid>=1.0.1       # controladores PID de PIDTuningStrategy
rich>=12.0.0            # interfaz de terminal (RichTerminalUI)
pyfiglet>=0.8.post1     # banner ASCII de la interfaz de terminal

# Todo lo demas que usa el proyecto es biblioteca estandar y no se instala:
# argparse, csv, http.server, json, logging, os, signal, socket, socketserver,
# statistics, sys, threading, time, typing, urllib.parse
